<UserControl x:Class="DisplayWpf.CanvasControl1"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="800">
    <Grid>
        <ScrollViewer Height="248" HorizontalAlignment="Left" Margin="400,52,0,0" Name="svForImg" VerticalAlignment="Top" Width="400" IsManipulationEnabled="False" PanningMode="Both" CanContentScroll="True" HorizontalScrollBarVisibility="Visible" />
        <Viewbox Height="248" HorizontalAlignment="Left" Margin="0,52,0,0" Name="vbForImg" VerticalAlignment="Top" Width="400" />
    </Grid>
</UserControl>
